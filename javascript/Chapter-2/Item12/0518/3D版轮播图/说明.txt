let temp1 = step - 2,
    temp2 = step - 1,
    temp3 = step,
    temp4 = step + 1,
    temp5 = step + 2;
if (temp1 < 0) temp1 = len + temp1;
if (temp2 < 0) temp2 = len + temp2;
if (temp4 > len - 1) temp4 = temp4 - len;
if (temp5 > len - 1) temp5 = temp5 - len;

translate(-50%, -50%) scale(0.55)   0
TEMP3 translate(-50%, -50%) scale(1)  3
TEMP1 translate(-195%, -50%) scale(0.7) 1
TEMP2 translate(-130%, -50%) scale(0.85) 2
TEMP4 translate(30%, -50%) scale(0.85) 2
TEMP5 translate(95%, -50%) scale(0.7) 1


确保获取的5个
let diff = source.length - 5;
if (diff < 0) {
    diff = Math.abs(diff);
    source.slice(0, diff).forEach(item => {
        source.push({
            ...item,
            id: parseInt(source[source.length - 1].id) + 1
        });
    });
}