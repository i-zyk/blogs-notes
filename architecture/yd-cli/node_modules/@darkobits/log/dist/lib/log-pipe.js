var __defProp = Object.defineProperty;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
import os from "node:os";
import { Writable } from "node:stream";
import stripAnsi from "strip-ansi";
const LAST_EOL_PATTERN = new RegExp(`${os.EOL}(?!.*${os.EOL})`);
class LogPipe extends Writable {
  constructor(logFn) {
    super();
    __publicField(this, "_logFn");
    if (typeof logFn !== "function")
      throw new Error(`[LogPipe] Expected type of first parameter to be "function", got "${typeof logFn}".`);
    this._logFn = logFn;
  }
  // eslint-disable-next-line @typescript-eslint/naming-convention
  _write(chunk, encoding, callback) {
    let str = Buffer.from(chunk).toString("utf8");
    if (str === "") {
      callback();
      return;
    }
    if (stripAnsi(str).endsWith(os.EOL)) {
      str = str.replace(LAST_EOL_PATTERN, "");
    }
    this._logFn(str);
    callback();
  }
}
export {
  LogPipe
};
//# sourceMappingURL=log-pipe.js.map
