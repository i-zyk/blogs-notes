"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getPackageInfo = void 0;
const fs_1 = __importDefault(require("fs"));
const path_1 = __importDefault(require("path"));
const fd_name_1 = require("@darkobits/fd-name");
const read_pkg_up_1 = __importDefault(require("read-pkg-up"));
/**
 * @private
 *
 * Module-local cache of package info lookups.
 */
const packageCache = new Map();
/**
 * Loads the package.json of the host or local package and returns the
 * normalized result and the package's root directory. Results are cached.
 */
function getPackageInfo(type) {
    // Cache miss; populate cache.
    if (!packageCache.has(type)) {
        const cwd = type === 'host'
            ? path_1.default.dirname(fs_1.default.realpathSync(process.argv[1]))
            : (0, fd_name_1.dirname)();
        if (!cwd)
            throw new Error(`[getOurPackageInfo] Unable to compute cwd for the ${type} package.`);
        const packageResult = read_pkg_up_1.default.sync({ cwd });
        if (!packageResult)
            throw new Error(`[getPackageInfo] Unable to get metadata for the ${type} package.`);
        packageCache.set(type, {
            json: packageResult.packageJson,
            root: path_1.default.dirname(packageResult.path)
        });
    }
    // Return from cache.
    return packageCache.get(type);
}
exports.getPackageInfo = getPackageInfo;
