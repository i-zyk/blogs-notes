"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const path_1 = __importDefault(require("path"));
const aggregate_error_1 = __importDefault(require("aggregate-error"));
const fs_extra_1 = __importDefault(require("fs-extra"));
const pkg_dir_1 = __importDefault(require("pkg-dir"));
const resolve_pkg_1 = __importDefault(require("resolve-pkg"));
const log_1 = __importDefault(require("../lib/log"));
/**
 * @private
 *
 * Creates a temporary module in the nearest node_modules folder that loads
 * ts-node and tsconfig-paths, then loads the provided configuration file, and
 * returns the results.
 */
async function withTsNode(pkgDir, filePath) {
    var _a;
    const tsNodePath = (0, resolve_pkg_1.default)('ts-node');
    const tsConfigPathsPath = (0, resolve_pkg_1.default)('tsconfig-paths');
    const wrapper = `
    require('${tsNodePath}').register({
      transpileOnly: true,
      compilerOptions: {
        module: 'commonjs'
      }
    });

    require('${tsConfigPathsPath}/register');

    const config = require("${filePath}");
    module.exports = config.default ?? config;
  `;
    const tempDir = path_1.default.resolve(pkgDir, 'node_modules', '.saffron-config');
    await fs_extra_1.default.ensureDir(tempDir);
    const loaderPath = path_1.default.resolve(tempDir, 'loader.js');
    await fs_extra_1.default.writeFile(loaderPath, wrapper);
    const result = await (_a = loaderPath, Promise.resolve().then(() => __importStar(require(_a))));
    await fs_extra_1.default.remove(tempDir);
    log_1.default.verbose(log_1.default.prefix('parseConfiguration'), log_1.default.chalk.green.bold('Loaded configuration using ts-node.'));
    return result;
}
/**
 * @private
 *
 * Creates a temporary module in the nearest node_modules folder that loads
 * ts-node and tsconfig-paths, then loads the provided configuration file, and
 * returns the results.
 */
async function withBabel(pkgDir, filePath) {
    var _a;
    const babelRegisterPath = (0, resolve_pkg_1.default)('@babel/register');
    const wrapper = `
    require('${babelRegisterPath}')({
      extensions: ['.ts', '.js', '.cjs']
    });

    const config = require("${filePath}");
    module.exports = config.default ?? config;
  `;
    const tempDir = path_1.default.resolve(pkgDir, 'node_modules', '.saffron-config');
    await fs_extra_1.default.ensureDir(tempDir);
    const loaderPath = path_1.default.resolve(tempDir, 'loader.js');
    await fs_extra_1.default.writeFile(loaderPath, wrapper);
    const result = await (_a = loaderPath, Promise.resolve().then(() => __importStar(require(_a))));
    await fs_extra_1.default.remove(tempDir);
    log_1.default.verbose(log_1.default.prefix('parseConfiguration'), log_1.default.chalk.green.bold('Loaded configuration using @babel/register.'));
    return result;
}
/**
 * Cosmiconfig custom loader that supports ESM syntax. It allows host
 * applications to be written in ESM or CJS, and for the consumers of those
 * applications to write configuration files as ESM or CJS.
 */
async function configurationLoader(filePath) {
    const errors = [];
    const pkgDir = await (0, pkg_dir_1.default)(path_1.default.dirname(filePath));
    if (!pkgDir)
        throw new Error(`${log_1.default.prefix('parseConfiguration')} Unable to compute package directory.`);
    try {
        const config = await withTsNode(pkgDir, filePath);
        return config?.default ?? config;
    }
    catch (err) {
        errors.push(new Error(`${log_1.default.prefix('parseConfiguration')} Failed to load configuration with ts-node: ${err}`));
    }
    try {
        const config = await withBabel(pkgDir, filePath);
        return config?.default ?? config;
    }
    catch (err) {
        errors.push(new Error(`${log_1.default.prefix('parseConfiguration')} Failed to load configuration with @babel/register: ${err}`));
    }
    if (errors.length > 0) {
        throw new aggregate_error_1.default(errors);
    }
}
exports.default = configurationLoader;
